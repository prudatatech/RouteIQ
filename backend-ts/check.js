const { createClient } = require('@supabase/supabase-js'); const dotenv = require('dotenv'); dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('users').select('language_preference').limit(1).then(({error}) => { console.log(error ? 'MISSING' : 'EXISTS'); process.exit(0); });
